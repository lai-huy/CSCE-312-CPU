.pos 0
sum: 
    irmovq $8, %r8 
    irmovq $1 ,%r9 
    xorq %rax,%rax 
    irmovq $5 ,%rsi 
    andq %rsi, %rsi
    jmp cond
loop:
    addq %r8 ,%rax 
    subq %r9,%rsi 
cond:
    jne loop 
    halt
 